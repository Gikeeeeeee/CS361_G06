[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: Fast unit tests with no external dependencies
    handler: Lambda handler event routing and validation tests
    integration: Integration tests between application components
    api: Live HTTP API tests against deployed API Gateway (requires API_BASE_URL)
